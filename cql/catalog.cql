CREATE TABLE catalog.movies (
    id uuid PRIMARY KEY,
    actor_1_name text,
    actor_2_name text,
    actor_3_name text,
    comb text,
    director_name text,
    genres text,
    movie_title text);